# Demo seed data — runs ONLY under the `seed` Liquibase context (dev stack sets it).
databaseChangeLog:
  - changeSet:
      id: 0.1.0-seed-gradebook
      author: codillas
      context: seed
      comment: Demo gradebook read-model rows (dev only)
      changes:
        - sqlFile:
            path: 0.1.0-seed-gradebook.sql
            relativeToChangelogFile: true
            splitStatements: true
            stripComments: false
