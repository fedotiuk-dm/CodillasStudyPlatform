# Files module changelog — SemVer 2.0.0: https://semver.org/spec/v2.0.0/
databaseChangeLog:
  # 0.1.0 — stored file metadata
  - include:
      file: changes/0.1.0/0.1.0-create-stored-files.yaml
      relativeToChangelogFile: true
  # 0.2.0 — lock_version on all tables (lock lives on BaseAuditableEntity)
  - include:
      file: changes/0.2.0/0.2.0-add-files-lock-version.yaml
      relativeToChangelogFile: true
  # 0.6.0 — polymorphic owner + uploader indexes
  - include:
      file: changes/0.6.0/0.6.0-add-files-indexes.yaml
      relativeToChangelogFile: true
