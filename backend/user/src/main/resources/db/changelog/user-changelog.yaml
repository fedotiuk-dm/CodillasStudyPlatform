# User module changelog — SemVer 2.0.0: https://semver.org/spec/v2.0.0/
# Includes versioned changesets in order. Each lives under changes/<version>/.
databaseChangeLog:
  # 0.1.0 — user profiles
  - include:
      file: changes/0.1.0/0.1.0-create-user-profiles.yaml
      relativeToChangelogFile: true
  # demo seed (context: seed)
  - include:
      file: changes/0.1.0/0.1.0-seed-user.yaml
      relativeToChangelogFile: true
  # 0.2.0 — capture email for the notification channel
  - include:
      file: changes/0.2.0/0.2.0-add-profile-email.yaml
      relativeToChangelogFile: true
  # 0.3.0 — lock_version on all tables (lock lives on BaseAuditableEntity)
  - include:
      file: changes/0.3.0/0.3.0-add-user-lock-version.yaml
      relativeToChangelogFile: true
  # 0.8.0 — demo profile emails (context: seed)
  - include:
      file: changes/0.8.0/0.8.0-seed-user-emails.yaml
      relativeToChangelogFile: true
