# Demo seed data — runs ONLY under the `seed` Liquibase context (dev stack sets it).
databaseChangeLog:
  - changeSet:
      id: 0.1.0-seed-homework
      author: codillas
      context: seed
      comment: Demo assignment with a graded submission (dev only)
      changes:
        - sqlFile:
            path: 0.1.0-seed-homework.sql
            relativeToChangelogFile: true
            splitStatements: true
            stripComments: false
