databaseChangeLog:
  - changeSet:
      id: 0.1.0-create-notification-memberships
      author: codillas
      comment: Create notification_memberships roster — unique per (group, student) (0.1.0)
      preConditions:
        - onFail: MARK_RAN
        - not:
            - tableExists:
                tableName: notification_memberships
      changes:
        - createTable:
            tableName: notification_memberships
            columns:
              - column:
                  name: id
                  type: uuid
                  constraints: { primaryKey: true, nullable: false }
              - column:
                  name: group_id
                  type: uuid
                  constraints: { nullable: false }
              - column:
                  name: student_id
                  type: uuid
                  constraints: { nullable: false }
              - column:
                  name: created_at
                  type: timestamptz
              - column:
                  name: updated_at
                  type: timestamptz
        - addUniqueConstraint:
            tableName: notification_memberships
            columnNames: group_id, student_id
            constraintName: uq_notification_memberships_group_student
