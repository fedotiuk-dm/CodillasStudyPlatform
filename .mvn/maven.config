# Parallel build configuration
# -T 1C = use 1 thread per CPU core
# This enables parallel module builds and parallel test execution
-T 1C

# Enable Maven build cache (Maven 3.9+)
# Speeds up builds by caching task outputs
-Dmaven.build.cache.enabled=true
